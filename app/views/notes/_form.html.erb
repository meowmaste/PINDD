<%= form_for(@note) do |f| %>
  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% @note.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="center">
  <div class="postit">

    <div class="field">
      <%= f.label :to %>
      <%= f.select :group_id, current_user.groups.map{ |g| [( g.default_group? ? "me" : g.name), g.id]} %>
    </div>
    <div class="field">
      <%= f.text_area :content%>
    </div>

  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
</div>
<% end %>
